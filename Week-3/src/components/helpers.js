//Now other file can export this
export const log = (message, timeStamp) => console.log(`${message} received at ${timeStamp}`)
export const PI = 3.14;
export const obj = {
    type:"furniture",
    price:10.99,
}
